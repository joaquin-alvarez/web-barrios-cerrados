---
---

<header class="hero">
  <div class="hero-content">
    <div class="logo">
      <img src="/logo.svg" alt="Centro de Proyectos y Gestión" />
    </div>
    <h1>
      <span class="small-heading">CENTRO DE</span>
      <span class="main-heading">Proyectos y Gestión</span>
      <span class="location">ESCOBAR</span>
    </h1>
    <p class="tagline">
      Articulamos proyectos entre lo social,<br />
      lo privado y lo público
    </p>
    <div class="scroll-indicator">
      <a href="#content">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"></circle>
          <polyline points="8 12 12 16 16 12"></polyline>
          <line x1="12" y1="8" x2="12" y2="16"></line>
        </svg>
      </a>
    </div>
  </div>
</header>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    background: linear-gradient(rgba(102, 114, 70, 0.7), rgba(102, 114, 70, 0.7)), 
                url('/background.jpg') no-repeat center center/cover;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
    padding: 0 20px;
  }

  .hero-content {
    max-width: 800px;
  }

  .logo {
    margin-bottom: 20px;
  }
  
  .logo img {
    width: 80px;
  }

  .small-heading {
    display: block;
    font-size: 1.2rem;
    font-weight: normal;
    letter-spacing: 2px;
  }

  .main-heading {
    display: block;
    font-size: 3.5rem;
    font-weight: bold;
    line-height: 1.1;
    margin: 10px 0;
  }

  .location {
    display: inline-block;
    font-size: 1rem;
    font-weight: normal;
    padding: 2px 8px;
    background: rgba(255, 255, 255, 0.8);
    color: var(--color-dark);
    border-radius: 4px;
  }

  .tagline {
    margin-top: 20px;
    font-size: 1.2rem;
    line-height: 1.5;
  }

  .scroll-indicator {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    animation: bounce 2s infinite;
  }

  .scroll-indicator a {
    color: white;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-20px);
    }
    60% {
      transform: translateY(-10px);
    }
  }

  @media (max-width: 768px) {
    .main-heading {
      font-size: 2.5rem;
    }
    
    .small-heading {
      font-size: 1rem;
    }
    
    .tagline {
      font-size: 1rem;
    }
  }
</style>
